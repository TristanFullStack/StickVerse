{% extends 'base.html.twig' %}
{% block title %}Modifier {{ passif.nom }} — Administration{% endblock %}
{% block body %}
    <div class="admin-page-heading"><div><p class="admin-kicker">Administration / Combat</p><h1>Modifier {{ passif.nom }}</h1><p>Les cartes équipées seront synchronisées après l’enregistrement.</p></div></div>
    <section class="admin-form-card">{{ include('passif/_form.html.twig', {'button_label': 'Enregistrer et synchroniser'}) }}</section>
    <p class="admin-back-link"><a href="{{ path('app_passif_index') }}">← Retour au catalogue des passifs</a></p>
{% endblock %}
