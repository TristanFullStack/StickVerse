{% extends 'base.html.twig' %}

{% block title %}Créer mon compte - StickVerse{% endblock %}

{% block body %}
    <h1>Créer mon compte</h1>

    <p>
        Ton compte recevra immédiatement quatre Stickmans différents
        pour composer sa première équipe.
    </p>
    <p>
        Après ton inscription, un lien de confirmation sera envoyé à ton adresse
        e-mail. La connexion sera disponible une fois l’adresse confirmée.
    </p>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.pseudo) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
        {{ form_row(registrationForm.agreeTerms, {
            label: 'J’accepte les conditions d’utilisation'
        }) }}

        <button type="submit" class="btn">Créer mon compte</button>
    {{ form_end(registrationForm) }}
{% endblock %}
