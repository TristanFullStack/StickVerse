{% extends 'base.html.twig' %}
{% block title %}{{ titre }} — Administration{% endblock %}
{% block body %}<div class="admin-page-heading"><div><p class="admin-kicker">Administration / Communication</p><h1>{{ titre }}</h1></div></div><section class="admin-form-panel">{{ form_start(form) }}{{ form_widget(form) }}<button class="admin-primary-action" type="submit">Enregistrer</button>{{ form_end(form) }}</section><p><a href="{{ path('app_actualite_admin_index') }}">← Retour aux actualités</a></p>{% endblock %}
