{{ form_start(form, {'attr': {'class': 'admin-form'}}) }}
    <div class="admin-form-fields">
        {{ form_widget(form) }}
    </div>
    <button class="admin-primary-action" type="submit">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
