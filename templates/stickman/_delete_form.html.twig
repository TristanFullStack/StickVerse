<form class="admin-delete-form" method="post" action="{{ path('app_stickman_delete', {'id': stickman.id}) }}" onsubmit="return confirm('Supprimer ce Stickman ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stickman.id) }}">
    <button class="admin-danger-action" type="submit">Supprimer</button>
</form>
