{% extends 'base.html.twig' %}

{% block title %}CaisseStickman{% endblock %}

{% block body %}
    <h1>CaisseStickman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caisse_stickman.id }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ caisse_stickman.poids }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_caisse_stickman_index') }}">back to list</a>

    <a href="{{ path('app_caisse_stickman_edit', {'id': caisse_stickman.id}) }}">edit</a>

    {{ include('caisse_stickman/_delete_form.html.twig') }}
{% endblock %}
