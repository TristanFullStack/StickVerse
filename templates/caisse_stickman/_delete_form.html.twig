<form class="admin-delete-form" method="post" action="{{ path('app_caisse_stickman_delete', {'id': caisse_stickman.id}) }}" onsubmit="return confirm('Retirer ce Stickman de la caisse ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caisse_stickman.id) }}">
    <button class="admin-danger-action" type="submit">Supprimer</button>
</form>
