<form method="post" action="{{ path('app_caisse_stickman_delete', {'id': caisse_stickman.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caisse_stickman.id) }}">
    <button class="btn">Delete</button>
</form>
