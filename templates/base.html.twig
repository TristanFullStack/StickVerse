<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}StickVerse{% endblock %}</title>
        <meta name="description" content="Collectionne des Stickmans, construis ton équipe et affronte d’autres joueurs. Découvre StickVerse, le jeu gratuit de collection et de stratégie.">
        <meta name="theme-color" content="#080f1b">
        <link rel="icon" href="{{ asset('images/brand/stickverse.svg') }}" type="image/svg+xml">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/design-system.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/cards.css') }}">
            <link rel="stylesheet" href="{{ asset('styles/pages.css') }}">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        {% set frankenphp_hot_reload = app.request.server.get('FRANKENPHP_HOT_RELOAD') %}
        {% if frankenphp_hot_reload %}
        <meta name="frankenphp-hot-reload:url" content="{{ frankenphp_hot_reload }}">
        <script src="https://cdn.jsdelivr.net/npm/idiomorph"></script>
        <script src="https://cdn.jsdelivr.net/npm/frankenphp-hot-reload/+esm" type="module"></script>
        {% endif %}
    </head>
    <body>
        {% set navigation_route = app.request.attributes.get('_route')|default('') %}

        <a class="skip-link" href="#main-content">Aller au contenu</a>
        <header class="site-header" data-site-header>
            <a class="site-name" href="{{ path('app_home') }}">
                <img src="{{ asset('images/brand/stickverse.svg') }}" alt="" width="32" height="32">
                Stick<span>Verse</span>
            </a>
            <button class="navigation-toggle" type="button" aria-expanded="false" aria-controls="main-navigation" data-navigation-toggle hidden>Menu <span aria-hidden="true">☰</span></button>

            <nav id="main-navigation" class="site-navigation" aria-label="Navigation principale">
                <a
                    href="{{ path('app_home') }}"
                    {% if navigation_route == 'app_home' %}aria-current="page"{% endif %}
                >Accueil</a>
                <a
                    href="{{ path('app_wiki') }}"
                    {% if navigation_route starts with 'app_wiki' %}aria-current="page"{% endif %}
                >Wiki</a>
                <a
                    href="{{ path('app_caisse_publique') }}"
                    {% if navigation_route in ['app_boutique', 'app_caisse_publique'] %}aria-current="page"{% endif %}
                >Boutique</a>
                <a
                    href="{{ path('app_actualite_index') }}"
                    {% if navigation_route starts with 'app_actualite' %}aria-current="page"{% endif %}
                >Actualités</a>

                {% if app.user %}
                    <a
                        href="{{ path('app_inventaire') }}"
                        {% if navigation_route == 'app_inventaire' %}aria-current="page"{% endif %}
                    >Inventaire</a>
                    <a
                        href="{{ path('app_collection') }}"
                        {% if navigation_route == 'app_collection' %}aria-current="page"{% endif %}
                    >Collection</a>
                    <a
                        href="{{ path('app_saison') }}"
                        {% if navigation_route == 'app_saison' %}aria-current="page"{% endif %}
                    >Saison</a>
                    <a
                        href="{{ path('app_equipe') }}"
                        {% if navigation_route == 'app_equipe' %}aria-current="page"{% endif %}
                    >Équipe</a>
                    <a
                        href="{{ path('app_combats_en_ligne') }}"
                        {% if navigation_route == 'app_combats_en_ligne' or navigation_route == 'app_rapport_combat_en_ligne' or navigation_route starts with 'app_combat_en_ligne_' %}aria-current="page"{% endif %}
                    >Combats</a>
                    <a
                        href="{{ path('app_recompenses') }}"
                        class="site-rewards-link"
                        {% if navigation_route == 'app_recompenses' %}aria-current="page"{% endif %}
                    >Récompenses{% if recompenses_disponibles(app.user) %} <span class="notification-bubble" title="Récompense disponible">!</span>{% endif %}</a>
                {% endif %}
            </nav>

            <div class="site-account">
                {% if app.user %}
                    <span>{{ app.user.pseudo }}</span>
                    <span data-solde-pieces>{{ app.user.pieces }} pièces</span>
                    <a
                        href="{{ path('app_profil') }}"
                        {% if navigation_route == 'app_profil' %}aria-current="page"{% endif %}
                    >Profil</a>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Connexion</a>
                    <a href="{{ path('app_register') }}">Inscription</a>
                {% endif %}
            </div>

            {% if is_granted('ROLE_ADMIN') %}
                <nav
                    class="site-admin-navigation"
                    aria-label="Administration"
                    data-navigation-admin
                >
                    <strong class="site-admin-label">Administration</strong>
                    <div class="site-admin-links">
                        <a href="{{ path('app_admin_console') }}" {% if navigation_route == 'app_admin_console' %}aria-current="page"{% endif %}>Console</a>
                        <a href="{{ path('app_stickman_index') }}" {% if navigation_route starts with 'app_stickman_' %}aria-current="page"{% endif %}>Stickmans</a>
                        <a href="{{ path('app_passif_index') }}" {% if navigation_route starts with 'app_passif_' %}aria-current="page"{% endif %}>Passifs</a>
                        <a href="{{ path('app_caisse_index') }}" {% if navigation_route starts with 'app_caisse_' and navigation_route != 'app_caisse_stickman_index' and navigation_route != 'app_caisse_stickman_new' and navigation_route != 'app_caisse_stickman_show' and navigation_route != 'app_caisse_stickman_edit' and navigation_route != 'app_caisse_stickman_delete' %}aria-current="page"{% endif %}>Caisses</a>
                        <a href="{{ path('app_collection_jeu_index') }}" {% if navigation_route starts with 'app_collection_jeu_' %}aria-current="page"{% endif %}>Collections</a>
                        <a href="{{ path('app_caisse_stickman_index') }}" {% if navigation_route starts with 'app_caisse_stickman_' %}aria-current="page"{% endif %}>Contenu des caisses</a>
                        <a href="{{ path('app_actualite_admin_index') }}" {% if navigation_route starts with 'app_actualite_admin_' %}aria-current="page"{% endif %}>Actualités</a>
                        <a href="{{ path('app_admin_recompenses') }}" {% if navigation_route == 'app_admin_recompenses' %}aria-current="page"{% endif %}>Historique récompenses</a>
                    </div>
                </nav>
            {% endif %}
        </header>

        <main id="main-content" class="site-content" tabindex="-1">
            {% block body %}{% endblock %}
        </main>
        <footer class="site-footer"><a href="{{ path('app_home') }}">StickVerse</a><span>Ta collection. Ton équipe. Ta stratégie.</span><a href="{{ path('app_wiki') }}">Explorer le Wiki</a></footer>
    </body>
</html>
