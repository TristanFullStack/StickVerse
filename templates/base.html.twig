<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}StickVerse{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        {% set frankenphp_hot_reload = app.request.server.get('FRANKENPHP_HOT_RELOAD') %}
        {% if frankenphp_hot_reload %}
        <meta name="frankenphp-hot-reload:url" content="{{ frankenphp_hot_reload }}">
        <script src="https://cdn.jsdelivr.net/npm/idiomorph"></script>
        <script src="https://cdn.jsdelivr.net/npm/frankenphp-hot-reload/+esm" type="module"></script>
        {% endif %}
    </head>
    <body>
        {% set navigation_route = app.request.attributes.get('_route')|default('') %}

        <header class="site-header">
            <a class="site-name" href="{{ path('app_home') }}">
                StickVerse
            </a>

            <nav class="site-navigation" aria-label="Navigation principale">
                <a
                    href="{{ path('app_home') }}"
                    {% if navigation_route == 'app_home' %}aria-current="page"{% endif %}
                >Accueil</a>
                <a
                    href="{{ path('app_wiki') }}"
                    {% if navigation_route starts with 'app_wiki' %}aria-current="page"{% endif %}
                >Wiki</a>
                <a
                    href="{{ path('app_caisse_publique') }}"
                    {% if navigation_route == 'app_caisse_publique' %}aria-current="page"{% endif %}
                >Caisses</a>

                {% if app.user %}
                    <a
                        href="{{ path('app_collection') }}"
                        {% if navigation_route == 'app_collection' %}aria-current="page"{% endif %}
                    >Collection</a>
                    <a
                        href="{{ path('app_equipe') }}"
                        {% if navigation_route == 'app_equipe' %}aria-current="page"{% endif %}
                    >Équipe</a>
                    <a
                        href="{{ path('app_combats_en_ligne') }}"
                        {% if navigation_route == 'app_combats_en_ligne' or navigation_route == 'app_rapport_combat_en_ligne' or navigation_route starts with 'app_combat_en_ligne_' %}aria-current="page"{% endif %}
                    >Combats</a>
                {% endif %}
            </nav>

            <div class="site-account">
                {% if app.user %}
                    <span>{{ app.user.pseudo }}</span>
                    <span data-solde-pieces>{{ app.user.pieces }} pièces</span>
                    <a
                        href="{{ path('app_profil') }}"
                        {% if navigation_route == 'app_profil' %}aria-current="page"{% endif %}
                    >Profil</a>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Connexion</a>
                    <a href="{{ path('app_register') }}">Inscription</a>
                {% endif %}
            </div>

            {% if is_granted('ROLE_ADMIN') %}
                <nav
                    class="site-admin-navigation"
                    aria-label="Administration"
                    data-navigation-admin
                >
                    <strong>Administration :</strong>
                    <a href="{{ path('app_admin_console') }}">Console</a>
                    <a href="{{ path('app_stickman_index') }}">Stickmans</a>
                    <a href="{{ path('app_caisse_index') }}">Caisses</a>
                    <a href="{{ path('app_caisse_stickman_index') }}">
                        Contenu des caisses
                    </a>
                </nav>
            {% endif %}
        </header>

        <div class="site-content">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
